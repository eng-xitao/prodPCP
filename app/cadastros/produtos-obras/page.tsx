"use client";
import { FormEvent, useEffect, useMemo, useState } from "react";
import "./page.css";

type Produto={id:string;codigo:string;descricao:string;unidade:string;classificacao:string;produtoAcabado:string};
type Cliente={id:string;cnpj:string;razaoSocial:string;nomeFantasia:string;obra:string;endereco:string;bairro:string;municipio:string;responsavel:string;telefone:string;tipoMaterial:string;clienteObra:string;tipoCadastro:string};
type Vinculo={id:string;codigoBelmonte:string;codigoFornecedorObra:string;unidade:string;descricao:string;obra:string;numeroPedido:string;tipoMaterial:string};
const PROD_KEY="prodpcp-produtos",CLI_KEY="prodpcp-clientes-fornecedores",KEY="prodpcp-produtos-obras";
const empty={codigoBelmonte:"",codigoFornecedorObra:"",unidade:"",descricao:"",obra:"",numeroPedido:"",tipoMaterial:""};
const norm=(v:string)=>v.trim().toLocaleLowerCase("pt-BR");
function properCase(v:string){return v.toLocaleLowerCase("pt-BR").trim().split(/\s+/).map((w,i)=>i>0&&["de","da","do","das","dos","e","em","com","por","para"].includes(w)?w:w.charAt(0).toLocaleUpperCase("pt-BR")+w.slice(1)).join(" ")}
export default function ProdutosPorObra(){
 const [form,setForm]=useState(empty),[products,setProducts]=useState<Produto[]>([]),[clients,setClients]=useState<Cliente[]>([]),[items,setItems]=useState<Vinculo[]>([]),[message,setMessage]=useState<string|null>(null),[editId,setEditId]=useState<string|null>(null),[search,setSearch]=useState("");
 useEffect(()=>{try{setProducts(JSON.parse(localStorage.getItem(PROD_KEY)||"[]"));setClients(JSON.parse(localStorage.getItem(CLI_KEY)||"[]"));setItems(JSON.parse(localStorage.getItem(KEY)||"[]"))}catch{}const id=new URLSearchParams(window.location.search).get("edit");if(id)try{const list:Vinculo[]=JSON.parse(localStorage.getItem(KEY)||"[]"),f=list.find(x=>x.id===id);if(f){setForm(f);setEditId(id)}}catch{}},[]);
 const obras=useMemo(()=>clients.filter(x=>x.tipoCadastro==="Obra"||x.clienteObra==="Obra"||x.obra).map(x=>x.obra||x.nomeFantasia).filter(Boolean).filter((v,i,a)=>a.findIndex(y=>norm(y)===norm(v))===i).sort((a,b)=>a.localeCompare(b,"pt-BR")),[clients]);
 const set=(k:keyof typeof empty,v:string)=>{setMessage(null);setForm(c=>({...c,[k]:v}))};
 const chooseProduct=(code:string)=>{const p=products.find(x=>norm(x.codigo)===norm(code));setForm(c=>({...c,codigoBelmonte:code,descricao:p?.descricao||"",unidade:p?.unidade||""}));setMessage(null)};
 const submit=(e:FormEvent)=>{e.preventDefault();if(!form.codigoBelmonte||!form.codigoFornecedorObra.trim()||!form.obra||!form.tipoMaterial)return setMessage("Preencha os campos obrigatórios.");const duplicate=items.some(x=>x.id!==editId&&norm(x.codigoBelmonte)===norm(form.codigoBelmonte)&&norm(x.obra)===norm(form.obra)&&norm(x.tipoMaterial)===norm(form.tipoMaterial));if(duplicate)return setMessage("Este produto já está vinculado a esta obra e material.");const p=products.find(x=>norm(x.codigo)===norm(form.codigoBelmonte));const record={...form,id:editId||crypto.randomUUID(),descricao:p?.descricao||form.descricao,unidade:p?.unidade||form.unidade,obra:properCase(form.obra),codigoFornecedorObra:form.codigoFornecedorObra.trim(),numeroPedido:form.numeroPedido.trim()};const next=editId?items.map(x=>x.id===editId?record:x):[...items,record];localStorage.setItem(KEY,JSON.stringify(next));setItems(next);setMessage(editId?"Vínculo atualizado com sucesso.":"Produto vinculado com sucesso.");setForm(empty);setEditId(null);window.history.replaceState({},"","/cadastros/produtos-obras")};
 const clear=()=>{setForm(empty);setEditId(null);setMessage(null);window.history.replaceState({},"","/cadastros/produtos-obras")};
 return <div className="app-shell"><aside className="sidebar"><img src="/prodpcp-logo.svg" alt="ProdPCP" className="logo"/><div className="menu-label">Cadastros</div><a className="menu-item" href="/cadastros/clientes-fornecedores"><span className="menu-icon">▦</span><span>Clientes e Fornecedores</span></a><a className="menu-item" href="/cadastros/produtos"><span className="menu-icon">▤</span><span>Produtos</span></a><a className="menu-item active" href="/cadastros/produtos-obras"><span className="menu-icon">⇄</span><span>Clientes x Produtos</span></a></aside><main className="main"><header className="topbar"><div className="breadcrumb">Cadastros &nbsp;/&nbsp; <strong>Clientes x Produtos</strong></div><div className="user-chip"><span className="avatar">PCP</span> ProdPCP</div></header><section className="content"><div className="page-heading"><h1>{editId?"Editar Cliente x Produto":"Cadastro de Clientes x Produtos"}</h1><p>Vincule produtos cadastrados às obras e mantenha os dados prontos para o Romaneio.</p></div><div className="card"><div className="card-header"><div><h2 className="card-title">Dados do vínculo</h2><div className="card-subtitle">Código BELMONTE, descrição e unidade são obtidos do Cadastro de Produtos.</div></div></div><form className="form" onSubmit={submit}><div className="grid"><div className="field"><label>Código BELMONTE <span className="required">*</span></label><select value={form.codigoBelmonte} onChange={e=>chooseProduct(e.target.value)}><option value="">Selecione o produto</option>{products.map(p=><option key={p.id} value={p.codigo}>{p.codigo} — {p.descricao}</option>)}</select></div><div className="field"><label>Cód. Produto (Forn/Obra) <span className="required">*</span></label><input value={form.codigoFornecedorObra} onChange={e=>set("codigoFornecedorObra",e.target.value)} /></div><div className="field"><label>Unidade de Medida</label><input value={form.unidade} readOnly /></div><div className="field span-8"><label>Descrição</label><input value={form.descricao} readOnly /></div><div className="field"><label>Obra <span className="required">*</span></label><select value={form.obra} onChange={e=>set("obra",e.target.value)}><option value="">Selecione a obra</option>{obras.map(o=><option key={o}>{o}</option>)}</select></div><div className="field"><label>Nº Ped.</label><input value={form.numeroPedido} onChange={e=>set("numeroPedido",e.target.value)} /></div><div className="field"><label>Tipo de Material <span className="required">*</span></label><select value={form.tipoMaterial} onChange={e=>set("tipoMaterial",e.target.value)}><option value="">Selecione</option><option>Ferro</option><option>Alumínio</option><option>Ambos</option></select></div></div><div className="actions"><button className="btn" type="button" onClick={clear}>Limpar</button><button className="btn btn-primary" type="submit">{editId?"Salvar alterações":"Cadastrar vínculo"}</button></div></form>{message&&<div className={`message ${message.includes("sucesso")?"success":"error"}`}>{message}</div>}</div><div className="card list-card"><div className="card-header"><div><h2 className="card-title">Pesquisa</h2><div className="card-subtitle">Pesquise por código, produto, obra, pedido ou material.</div></div></div><div className="search-input"><span>⌕</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Pesquisar..."/></div><div className="table-wrap"><table><thead><tr><th>Cód. BELMONTE</th><th>Cód. Forn/Obra</th><th>Descrição</th><th>Obra</th><th>Nº Ped.</th><th>Material</th><th></th></tr></thead><tbody>{items.filter(x=>{const q=norm(search);return !q||[x.codigoBelmonte,x.codigoFornecedorObra,x.descricao,x.obra,x.numeroPedido,x.tipoMaterial].some(v=>norm(v).includes(q))}).map(x=><tr key={x.id}><td><strong>{x.codigoBelmonte}</strong></td><td>{x.codigoFornecedorObra}</td><td>{x.descricao}</td><td>{x.obra}</td><td>{x.numeroPedido||"—"}</td><td>{x.tipoMaterial}</td><td><a className="edit-link" href={`/cadastros/produtos-obras?edit=${x.id}`}>Editar</a></td></tr>)}</tbody></table></div></div></section></main></div>
}